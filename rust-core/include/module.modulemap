module SideInstallerFFI {
    // Our own FFI surface (pairing host, logging, ping).
    header "sideinstaller.h"
    // idevice's full C-FFI (connection/tunnel/install), re-exported from our
    // staticlib via `extern crate idevice_ffi`. Generated by cbindgen at the
    // pinned rev; self-contained (plist.h appended).
    header "idevice.h"
    export *
}
